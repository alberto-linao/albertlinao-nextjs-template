# .git should be initialized by each dev to their own github account
.git/
Dockerfile
**/*.sql

# node_modules will be automatically installed by docker postCreateCommand
node_modules

# .env should be created by each dev to their own .env file
.env